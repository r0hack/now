{
  "name": "nuxtjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt --port $PORT",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "snyk-protect": "snyk protect",
    "prepare": "yarn run snyk-protect"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0",
    "snyk": "^1.316.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "snyk": true
}
